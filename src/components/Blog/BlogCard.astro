---
interface Props {
  title: string;
  excerpt: string;
  date: string;
  readTime?: string;
  tags?: string[];
  href?: string;
  pin?: boolean;
}

const { title, excerpt, date, readTime, tags = [], href = '#', pin = false } = Astro.props as Props;

const formattedDate = (() => {
  const d = new Date(date);
  return isNaN(d.getTime()) ? date : d.toLocaleDateString(undefined, { year: 'numeric', month: 'short', day: '2-digit' });
})();
---

<article class="relative image-border2 hover:hov-fx rounded-2 overflow-hidden transition-all duration-300 ease-in-out w-full h-full flex flex-col bg-black/40">
  <div class="h-1.5 w-full bg-gradient-to-r from-main via-secondary to-tertiary" />

  {pin && (
    <div class="absolute top-3 right-3 bg-black/60 rounded-full p-1.5 z-20 shadow-sm border border-white/10 backdrop-blur-sm" title="Pinned Blog">
      <div class="i-ri-pushpin-fill w-4 h-4 text-white"></div>
    </div>
  )}

  <div class="p-3 flex flex-col gap-2 flex-grow">
    <header class="flex items-start">
      <h3 class="text-base font-bold text-white leading-snug">
        <a href={href} class="link-fx-white font-bold">{title}</a>
      </h3>
    </header>

    <p class="text-sm text-white/90 leading-snug mt-1">{excerpt}</p>

    <div class="mt-auto flex flex-col">
      {tags.length > 0 && (
        <div class="flex flex-wrap gap-1 mt-1">
          {tags.map(t => (
            <span class="badge badge-active">{t}</span>
          ))}
        </div>
      )}

      <footer class="mt-2 flex items-center justify-between">
        <span class="text-xs text-gray-text">{readTime || ''}</span>
        <span class="text-xs text-gray-text">{formattedDate}</span>
      </footer>
    </div>
  </div>
</article>

