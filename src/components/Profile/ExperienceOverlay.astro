---
import { experiences } from '@data/experiences.ts';
import Overlay from '@components/Overlay.astro';
---

<Overlay id="experiences-overlay" title=".Experiences" closeButtonId="hide-all-experiences">
  <div class="space-y-2">
    {experiences.map(exp => (
      <article class="border-2 border-secondary/30 bg-black/70 rounded-lg p-2">
        <div class="flex items-center gap-2 min-w-0">
          <p class="prose-text font-semibold leading-tight truncate">{exp.company}</p>
          <span class="text-[11px] px-2 py-0.5 rounded-full bg-main/15 text-main border border-main/40 shrink-0">{exp.type}</span>
          <span class="text-[11px] px-2 py-0.5 rounded-full border border-secondary/40 text-gray-200 shrink-0">{exp.location}</span>
          <span class="ml-auto text-xs text-gray-400 whitespace-nowrap">{exp.period}</span>
        </div>
        {exp.role && (
          <p class="text-xs text-gray-300 mt-0.5">{exp.role}</p>
        )}
        {exp.points?.length > 0 && (
          <ul class="mt-2 list-disc list-inside space-y-1">
            {exp.points.map(pt => (
              <li class="text-sm text-white/90 leading-snug">{pt}</li>
            ))}
          </ul>
        )}
      </article>
    ))}
  </div>
</Overlay>
