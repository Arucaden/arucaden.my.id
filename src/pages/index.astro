---
import Layout from '../layouts/Layout.astro';
import supabase from '../lib/supabaseClient';

const { data: posts, error } = await supabase
  .from('blog')
  .select('*');

if (error) {
  console.error(error);
}
---
@tailwind base;
@tailwind components;
@tailwind utilities;

<Layout title="Welcome to Astro.">
	<main>
		<h1 class="bg-blue-500 text-white">Blog Posts</h1>
		<ul>
			{posts.map(blog => (
				<li key={blog.id}>{blog.content}</li>
			))}
			</ul>
	</main>
</Layout>

<style>

</style>
